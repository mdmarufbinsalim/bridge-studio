{
  "name": "bridge-audio",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "dev:desktop": "pnpm --filter desktop start:dev",
    "test-client": "pnpm --filter desktop test-client",
    "release:android": "bash scripts/release-android.sh"
  }
}
